/**
 * ChatPage — 主对话界面
 * Step 1.4 中将实现完整功能
 */
export default function ChatPage() {
  return (
    <div className="flex h-screen items-center justify-center">
      <div className="text-center">
        <h1 className="text-3xl font-bold text-foreground">💬 对话界面</h1>
        <p className="mt-2 text-muted-foreground">与 AI Agent 进行学术写作对话</p>
        <p className="mt-4 text-sm text-muted-foreground/60">将在 Step 1.4 中实现</p>
      </div>
    </div>
  )
}
